{{- if not .Values.singleNode.enabled }}
{{- if .Values.replicationManager.persistence.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "zero-cache.fullname" . }}-replication-manager
  labels:
    {{- include "zero-cache.labels" . | nindent 4 }}
    app.kubernetes.io/component: replication-manager
  annotations:
    {{- with .Values.replicationManager.persistence.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- if .Values.replicationManager.persistence.retainPolicy }}
    "helm.sh/resource-policy": {{ .Values.replicationManager.persistence.retainPolicy | quote }}
    {{- end }}
spec:
  accessModes:
    - {{ .Values.replicationManager.persistence.accessMode }}
  {{- if .Values.replicationManager.persistence.storageClass }}
  storageClassName: {{ .Values.replicationManager.persistence.storageClass }}
  {{- end }}
  {{- if .Values.replicationManager.persistence.allowVolumeExpansion }}
  volumeMode: Filesystem
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.replicationManager.persistence.size }}
{{- end }}

---
{{- if and .Values.viewSyncer.persistence.enabled (not .Values.viewSyncer.persistence.useVolumeClaimTemplates) }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "zero-cache.fullname" . }}-view-syncer
  labels:
    {{- include "zero-cache.labels" . | nindent 4 }}
    app.kubernetes.io/component: view-syncer
  annotations:
    {{- with .Values.viewSyncer.persistence.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- if .Values.viewSyncer.persistence.retainPolicy }}
    "helm.sh/resource-policy": {{ .Values.viewSyncer.persistence.retainPolicy | quote }}
    {{- end }}
spec:
  accessModes:
    - {{ .Values.viewSyncer.persistence.accessMode }}
  {{- if .Values.viewSyncer.persistence.storageClass }}
  storageClassName: {{ .Values.viewSyncer.persistence.storageClass }}
  {{- end }}
  {{- if .Values.viewSyncer.persistence.allowVolumeExpansion }}
  volumeMode: Filesystem
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.viewSyncer.persistence.size }}
{{- end }}
{{- else }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "zero-cache.fullname" . }}
  labels:
    {{- include "zero-cache.labels" . | nindent 4 }}
    app.kubernetes.io/component: single-node
spec:
  accessModes:
    - {{ .Values.singleNode.persistence.accessMode }}
  {{- if .Values.singleNode.persistence.storageClass }}
  storageClassName: {{ .Values.singleNode.persistence.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.singleNode.persistence.size }}
{{- end }}