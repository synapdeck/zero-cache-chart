# Example values.yaml for a single-node deployment of zero-cache

common:
  auth:
    secret:
      value: "your-secret-key"

  database:
    upstream:
      url:
        value: "postgresql://user:password@postgres-host:5432/postgres"

singleNode:
  enabled: true
